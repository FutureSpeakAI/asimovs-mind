flask>=3.0
anthropic>=0.40
google-genai>=1.0
rich>=13.0
colorama>=0.4
pyautogui>=0.9
beautifulsoup4>=4.12
requests>=2.31
pyyaml>=6.0
sentence-transformers>=2.2   # local embeddings for semantic context pruning (all-MiniLM-L6-v2)
headroom-ai[all]>=0.22       # Headroom context compression (Tejas Chopra, Apache 2.0) — https://github.com/chopratejas/headroom
headroom-ai[all]>=0.22       # Headroom context compression (Tejas Chopra, Apache 2.0) — https://github.com/chopratejas/headroom
